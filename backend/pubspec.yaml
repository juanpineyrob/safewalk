name: safewalk_backend
description: SafeWalk REST API (auth + persistencia Postgres).
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  postgres: ^3.3.0
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^2.14.0

dev_dependencies:
  lints: ^4.0.0
